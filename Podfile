# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TuistPractice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TuistPractice
  pod 'TensorFlowLiteSwift', '~> 2.14.0', :subspecs => ['Metal']

  target 'TuistPracticeTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
